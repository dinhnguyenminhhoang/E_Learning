class userProgressData {
  constructor(userId, learningPathId, progressPercentage, lastAccessed) {
    this.userId = userId;
    this.learningPathId = learningPathId;
    this.progressPercentage = progressPercentage;
    this.lastAccessed = lastAccessed;
  }
}
